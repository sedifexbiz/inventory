rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMP for development: allow any signed-in user
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
